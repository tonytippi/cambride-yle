import { describe, expect, it } from "vitest";
import { generationRequestSchema, mediaDraftSchema } from "@/features/content/domain/contracts";
import { outputHash } from "@/features/content/domain/provenance";
const base = { paper: "reading_writing", part: 1, engine: "picture_true_false", primaryTargetId: "018f0000-0000-7000-8000-000000000002", supportingTargetIds: [], topicIds: ["018f0000-0000-7000-8000-000000000003"], guidanceId: "018f0000-0000-7000-8000-000000000004", estimatedDurationSeconds: 60, accessibilityMetadata: { altText: "Cat" }, provenance: { source: "Original", rightsReference: "Owned" } };
describe("content contracts", () => { it("enforces text-to-question and image-to-image-media requests", () => { expect(generationRequestSchema.safeParse({ kind: "text", staffPrompt: "Draft", permittedReferences: [], draft: { ...base, mediaType: "image", description: "Cat" } }).success).toBe(false); expect(generationRequestSchema.safeParse({ kind: "image", staffPrompt: "Draft", permittedReferences: [], draft: { ...base, answerPolicyVersionId: "018f0000-0000-7000-8000-000000000005", prompt: "Cat", options: ["true"] } }).success).toBe(false); expect(generationRequestSchema.safeParse({ kind: "image", staffPrompt: "Draft", permittedReferences: [], draft: { ...base, mediaType: "audio", description: "Cat audio" } }).success).toBe(false); expect(generationRequestSchema.safeParse({ kind: "image", staffPrompt: "Draft", permittedReferences: [], draft: { ...base, mediaType: "image", description: "Cat" } }).success).toBe(true); }); it("keeps manual audio media valid and rejects signed preview URLs", () => { expect(mediaDraftSchema.safeParse({ ...base, mediaType: "audio", description: "Cat audio" }).success).toBe(true); expect(mediaDraftSchema.safeParse({ ...base, mediaType: "image", description: "Cat", previewUrl: "https://image.example.test/cat.png?signature=secret" }).success).toBe(false); expect(outputHash({ b: 2, a: { y: 2, x: 1 } })).toBe(outputHash({ a: { x: 1, y: 2 }, b: 2 })); }); });
